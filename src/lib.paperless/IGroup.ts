export interface IGroupAttributes 
{
	/**
	 * Setting removable to false will deactivate the ability to [[detach]] the [[Group]] from the [[Context]].
	 * 
	 * @todo						Not usable, need to be implemented.
	 */
	removable?: boolean
}
